@using cihaztakip.WebUI.Models;
@model List<NavDescriptionModel>

<div class="text-center">
    <h1 class="display-4">Anasayfa</h1>
</div>

<div class="container mt-5">
    <div class="row">
        @foreach (NavDescriptionModel navmodel in Model)
        {
            @(await Html.PartialAsync("_NavigationDescriptionCard", navmodel))
        }
    </div>
</div>